.git
node_modules
.next
out

# Local env files must never reach the image: NEXT_PUBLIC_* values are baked in
# at build time and come from build args, not from a developer's machine.
.env
.env.*

# Local artefacts, never build inputs.
logs/
*.log
*.png
tsconfig.tsbuildinfo
*.md
